page.10 {
    settings {
        colors {
            primary = {$site_package.configuration.color.primary}
            secondary = {$site_package.configuration.color.secondary}
            tertiary = {$site_package.configuration.color.tertiary}
            spot = {$site_package.configuration.color.spot}
            black = {$site_package.configuration.color.black}
            white = {$site_package.configuration.color.white}
        }
    }
}